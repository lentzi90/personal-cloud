apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: blog

commonLabels:
  environment: jern.me

bases:
  - ../../base

patchesStrategicMerge:
- ingress.yaml

resources:
- namespace.yaml
- ingress.yaml
