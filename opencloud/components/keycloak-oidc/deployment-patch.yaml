apiVersion: apps/v1
kind: Deployment
metadata:
  name: opencloud
spec:
  template:
    spec:
      volumes:
      - name: csp-config
        configMap:
          name: opencloud-csp
      containers:
      - name: opencloud
        env:
        - name: PROXY_CSP_CONFIG_FILE_LOCATION
          value: /etc/opencloud/csp.yaml
        volumeMounts:
        - name: csp-config
          mountPath: /etc/opencloud/csp.yaml
          subPath: csp.yaml
