apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nextcloud

commonLabels:
  app.kubernetes.io/name: nextcloud

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - nextcloud-data-pvc.yaml
