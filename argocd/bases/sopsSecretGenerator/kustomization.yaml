apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../upstream

images:
  - name: alpine
    newTag: 3.16.2

patchesStrategicMerge:
  - argocd-repo-server-deployment.yaml
  - argocd-cm.yaml

configMapGenerator:
  - name: sops-secret-generator-env
    literals:
      - SOPS_SECRET_GENERATOR_VERSION=v1.5.0
      - SOPS_VERSION=v3.7.3
      - PLATFORM=linux
