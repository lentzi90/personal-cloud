apiVersion: kubegres.reactive-tech.io/v1
kind: Kubegres
metadata:
  name: kubegres
spec:
   replicas: 2
   failover:
     isDisabled: true
     promotePod: kubegres-7-0
   backup:
    schedule: "0 1 * * *"
    pvcName: kubegres-backup
    volumeMount: /var/lib/backup
