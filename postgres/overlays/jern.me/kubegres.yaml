apiVersion: kubegres.reactive-tech.io/v1
kind: Kubegres
metadata:
  name: kubegres
spec:
   replicas: 2
   failover:
     isDisabled: true
   backup:
    schedule: "0 1 * * *"
    pvcName: kubegres-backup
    volumeMount: /var/lib/backup
