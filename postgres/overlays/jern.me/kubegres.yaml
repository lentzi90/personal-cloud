apiVersion: kubegres.reactive-tech.io/v1
kind: Kubegres
metadata:
  name: kubegres
spec:
   replicas: 2
   backup:
    schedule: "0 1 * * *"
    pvcName: my-backup-pvc
    volumeMount: /var/lib/backup
