apiVersion: kubegres.reactive-tech.io/v1
kind: Kubegres
metadata:
  name: kubegres
spec:
   replicas: 2
   backup:
    schedule: "0 1 * * *"
    pvcName: kubegres-backup
    volumeMount: /var/lib/backup
  resources:
    requests:
      memory: "100Mi"
      cpu: "800m"
