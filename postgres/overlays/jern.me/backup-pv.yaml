apiVersion: v1
kind: PersistentVolume
metadata:
  name: kubegres-backup
spec:
  capacity:
    storage: 200Mi
  accessModes:
    - ReadWriteMany
  nfs:
    server: nasse.lan
    path: "/Public/kubegres"
