apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kubegres

commonLabels:
  environment: kind

bases:
  - ../../bases/kubegres

resources:
  - namespace.yaml
  - backup-pvc.yaml

patchesStrategicMerge:
  - kubegres.yaml

generators:
  - postgres-credentials-generator.yaml
