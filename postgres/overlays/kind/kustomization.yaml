apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kubegres

commonLabels:
  environment: kind

resources:
  - ../../bases/kubegres
  - namespace.yaml
  - backup-pvc.yaml

patches:
  - path: kubegres.yaml

generators:
  - postgres-credentials-generator.yaml
