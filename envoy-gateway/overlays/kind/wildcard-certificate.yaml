apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-local
spec:
  secretName: wildcard-local-cert
  dnsNames:
  - "*.local"
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: selfsigned
  revisionHistoryLimit: 5
