apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../../../hetzner-acme/base
labels:
- pairs:
    environment: turing-talos
generatorOptions:
  disableNameSuffixHash: true
secretGenerator:
- name: hetzner-secret
  literals:
  - api-key=FAKE
